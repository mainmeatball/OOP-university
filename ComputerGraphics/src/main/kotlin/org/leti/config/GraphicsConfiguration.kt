package org.leti.config

import java.awt.Dimension

/**
 * @author sstepanov
 */
const val WIDTH = 800
const val HEIGHT = 600
const val CENTER_X = WIDTH / 2
const val CENTER_Y = HEIGHT / 2
val defaultDimension = Dimension(WIDTH, HEIGHT)
val defaultDimensionWithMenu = Dimension(WIDTH + 200, HEIGHT)